import { Meteor } from 'meteor/meteor';
import '../imports/api/entities.js'
import '../imports/api/events.js'
import '../imports/api/stories.js'


Meteor.startup(() => {
  // code to run on server at startup
});
